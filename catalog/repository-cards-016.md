# Repository Cards — Batch 016

Snapshot: 2026-08-30

This batch is descriptive only. No portfolio decisions are made here.

## Loofy147/Maintenance-life
A PHP enterprise-style maintenance-mode system. The repository exposes API/architecture/checklist/developer/security documentation, Docker support, and a `src`/`tests` surface. README describes health checks, admin dashboard, circuit breaker, buffered metrics, schema-validated configuration, adaptive cache, CLI, and DDD/layered architecture. The implementation claims are not independently validated by this card.

## Loofy147/TML-Virtual-Machine
Small repository with a nontrivial internal surface despite a minimal README. Contains `src`, `tests`, a custom `omniscience_seed.fso`, and `jules.md`. This indicates a virtual-machine/interpreter or execution-framework line with FSO-specific artifacts; exact runtime semantics require source-level inspection.

## Loofy147/One-handed
An umbrella repository called `agentdit-` in its README. It organizes assets across AI automation, computer vision, quantitative finance, scientific AI, LLM engineering, dataset businesses, Arabic AI, hidden-value discovery, and high-leverage research. It also exposes agent definitions, datasets, automation, CV, and research directories. Treat as an ecosystem/catalog container rather than assuming it is one product.

## Loofy147/Agents-next-tokens
README is tiny, but the repository contains substantial design and implementation artifacts: `BEST_PRACTICES.md`, `BUSINESS_STRATEGY.md`, `DATA_STRATEGY.md`, `DOCUMENTATION.md`, `IMPLEMENTATION_PLAN.md`, `QUALITY_ASSURANCE.md`, `ROADMAP.md`, skill registry JSON, `hiro_agent.py`, `competition_skill.py`, `kaggle_manager.py`, and quality-management code. This is an agent/skill system with both product strategy and executable artifacts.

## Loofy147/Symlib
Mathematical/algorithmic library centered on global structure in highly symmetric systems. README describes a short-exact-sequence framing, a discrete 4-torus model, intelligence fibers, counting formulas, parity obstruction, canonical seeds, a Python API, and 195 claimed passing tests. The mathematics and test claims remain evidence for later verification, not settled facts.

## Loofy147/Ai-taxonomy
README identifies this as the Procedure Intelligence Framework (PIF): a taxonomy and execution-safety architecture for software procedures in autonomous agents. It describes Hoare logic, weakest preconditions, procedure/DAG review, MCP contracts, context-optimization patterns, HITL boundaries, rollback semantics, and PIVS metrics, with a Python package layout (`executor`, `metrics`, `models`, `review`, `router`, `schema`, `verification`) plus JSON schemas and tests. This repository is potentially relevant to the evidence/governance line, but no relationship decision is made here.

## Loofy147/Claude-genieune
README is minimal, but the root contains `FINAL_RAPPORT.json`, Kaggle benchmark metadata/population documents, a large `benchmark_results.json`, causal-ablation code/results, notebook-generation scripts, and Python artifacts. Therefore it is a benchmark/research artifact repository rather than an empty or documentation-only project.

## Loofy147/Deal-Stream
A TypeScript workspace/application surface using pnpm and Replit configuration. Root contains `artifacts`, `lib`, `scripts`, `package.json`, `pnpm-workspace.yaml`, a large lockfile, and TypeScript configuration. The root does not expose a README through the current API surface, so the exact product purpose remains unresolved; the implementation surface is clearly nontrivial.

## Loofy147/The-new-era
AI Operating System Framework. README describes an extensible system-composition environment containing agent manifests, CLI, React dashboard, docs, infrastructure, prompts, protocols, services, shared definitions, scripts, and tests. Intended to coordinate LLMs/agents with development tools and autonomous collaboration; exact implemented scope still requires source inspection.

## Loofy147/TNN-ARC-AGI-2026
ARC-AGI solver repository with `src`, `data`, and `docs`. README claims a hybrid symmetry/color-logic solver, topological invariants, sub-second rule discovery, and operational competition readiness. The repository structure supports an implementation/test target, but the numerical and competition claims are not accepted as verified from README alone.

## Loofy147/Ai-hichem
On-premise AI coding assistant on branch `feature/initial-project-setup`. README specifies FastAPI backend, local inference, semantic code search, VS Code TypeScript extension, Docker/Prometheus/Grafana infrastructure, model update support, and agent extensibility. This is a concrete full-stack/local-AI architecture rather than a concept-only repository.

## Loofy147/Research-framwork
AI Agent Meta-Orchestrator framework. README defines a TypeScript/Node.js CLI framework for controlled experiments over `AgentVariant` implementations, YAML-driven configurations, reporting, Vitest, and an orchestrator/registry architecture. It is explicitly experiment-oriented and should be distinguished from a production agent platform.

## Loofy147/cognitive-dissonance
Substantial research/application repository. Root contains environment example, GitHub configuration, pre-commit/secrets baseline, Docker Compose, charts, data, demo, docs, infrastructure, and an embedded `early-diabetes-classification.zip`. This is clearly more than a README-level project; exact domain composition needs deeper source inspection.

## Loofy147/Goat
Layered AI/ML system. Root includes `core`, `business`, `knowledge`, `skills`, `layers`, and `interface`, along with CatBoost artifacts, competitions, dataset metadata, and rerun/pre-commit tooling. The repository appears to combine model/training capabilities with a broader agent/system architecture; precise boundaries remain open.

## Loofy147/call-centre
Concrete Algerian call-center system surface. Root includes a dataset CSV, `call_router.py`, configuration, Docker/Docker Compose, evaluation scripts/results, and an agent-specific Dockerfile. Presence of a committed `.env` is a security/provenance signal; secret contents were not inspected or reproduced.

## Loofy147/Exchange
Python-heavy multi-module repository with `MANIFEST.md`, `exchange.py`, `auditor.py`, `arriving.py`, `being.py`, `kairos.py`, `meta.py`, `aion_full.py`, `jules.py`, and a `this.py` module. The code naming suggests an experimental meta/agentic system, but function-level meaning and inter-module dependency must be established from source rather than filenames.

## Loofy147/STRATOS_OMEGA_ULTIMATE_V4
Artifact-heavy V4 STRATOS repository. Root contains multiple large ZIP archives including `STRATOS_MANIFOLD_COMPREHENSIVE (7)`, `STRATOS_MANIFOLD_FINAL_V4_2048D`, and an `UPGRADED` variant, plus `STRATOS_OMEGA_SUPREME.py` and a technical specification. This repository is especially important for version/artifact lineage because multiple packaged generations coexist in one tree.

## Evidence notes
- `TML-Virtual-Machine` demonstrates again that a short README can coexist with custom source/tests/artifacts. fileciteturn186file0
- `One-handed` explicitly describes itself as an agent asset ecosystem and exposes multiple specialized asset domains. fileciteturn187file0
- `Maintenance-life` contains a documented enterprise maintenance-system architecture, including API, architecture, checklist, developer guide, Docker and roadmap material. fileciteturn185file0 fileciteturn188file0
- `Research-framwork` explicitly defines the meta-orchestrator/agent-variant experiment model and CLI flow. fileciteturn189file0
- `SPEC-BPE` has a Python package, examples, scripts, requirements, and tests. fileciteturn190file0
- `Symlib` presents explicit mathematical structures and test claims that will require independent verification. fileciteturn194file0
- `Ai-taxonomy` presents a broad formal verification and procedure-intelligence design; the implementation claims must still be separated from the specification. fileciteturn195file0
- `Ai-hichem` identifies a concrete FastAPI + VS Code extension + local infrastructure architecture. fileciteturn196file0
- `Deal-Stream` has a nontrivial TypeScript workspace/build surface even though a README was not available through the current path. fileciteturn197file0
- `The-new-era` defines an AI Operating System composition framework with agents, services, protocols, infrastructure and tests. fileciteturn198file0
- `Claude-genieune` contains substantial benchmark and causal-ablation artifacts despite its 17-byte README. fileciteturn199file0 fileciteturn200file0
- `TNN-ARC-AGI-2026` has actual `src`, `data`, and `docs` directories behind its concise README. fileciteturn192file0 fileciteturn193file0
