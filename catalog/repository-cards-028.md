# Repository Inventory — Batch 028

Snapshot date: 2026-08-30

Scope: repositories newly surfaced during the current census pass. This batch is descriptive only; no keep/merge/archive decisions are made here.

## 1. Meta-meta
- Default branch: `main`
- Observed identity: autonomous/event-driven cryptocurrency trading signal system.
- Surface observed: modular signal strategies, event-driven ingestion/resampling, signal aggregation, portfolio/execution loop, performance tracking, risk validation, LLM chief analyst, Docker/Compose, TimescaleDB/Redis Streams.
- Evidence: README explicitly describes the seven major service areas and testing/backtesting commands.
- Status: substantial implementation candidate; source-level execution and test evidence still require direct inspection.
- Lineage note: name alone is not enough to relate this to other trading repositories.

## 2. Symmetric-HAG
- Default branch: `main`
- README points to `Global-theorem-` and `HAG`.
- Observed identity: linkage/alias repository for the symmetric-HAG work.
- Status: relationship signal recorded; content depth must be determined from tree/history.

## 3. Ket-apk
- Default branch: `main`
- README observed only contains repository title.
- Status: minimal observed documentation; do not infer emptiness without broader tree/history inspection.

## 4. Marchants
- Default branch: `main`
- No README available at the observed path.
- Root contains `.replit`, Expo-style `app.json`, `client/`, `server/`, `shared/`, assets, Drizzle configuration, ESLint, a 559KB package lock and design guidelines.
- Status: substantial full-stack application surface; product identity remains to be established from nested source.

## 5. Twin-ai
- Default branch: `main`
- Root contains `mobile/`, `web/`, `shared/`, `supabase/`, `scripts/`, `tests/`, `docs/`, `.jules`, and `AGENTS.md`; package.json exists but is only 60 bytes.
- Status: multi-surface application/research workspace; runtime purpose needs source inspection.

## 6. NeuraGrid
- Default branch: `main`
- README only says `NeuraGrid`.
- Root contains `PITCH_DECK.md` (~8KB) and `neurogrid-demo/`.
- Status: concept/product artifact plus demo surface; implementation depth remains open.

## 7. Gemini-app
- Default branch: `main`
- Identity: web application around Gemini CLI with GitHub integration.
- Features described: web terminal, realtime WebSocket output, PR/workflow interaction, server-side session storage for Gemini and GitHub credentials, and a `gemini-cli/` submodule.
- Status: implementation candidate; security model and source execution remain open.

## 8. Vipers-
- Default branch: `main`
- README contains only repository title.
- Root contains `backend/` and `frontend/`.
- Status: full-stack surface despite minimal README; nested implementation required.

## 9. NeuroGolf
- Default branch: `main`
- README is effectively empty.
- Root contains `core/`, `kernels/`, `registry/`, `tests/`, `produce_submission.py`, plus a checked-in `__pycache__/` directory.
- Status: real Python/research implementation surface; challenge/submission semantics require inspection.

## 10. Grok-supreme
- Default branch: `main`
- Identity: SSO-TS dashboard / cryptocurrency trading strategy simulator using xAI Grok and an OMEGA Framework integration.
- Surface described: Next.js/React UI, API routes for BTC price/backtest/Grok, skill orchestrator, Kaggle-based training, data/weights, docs, Python ML tooling.
- Explicit boundary: simulation-only, not real trade execution.
- README reports current performance metrics and targets; these are claims until independently reproduced.

## 11. escapement-learning-edition
- Default branch: `main`
- Root contains `client/`, `docs/`, `drizzle/`, `e2e/`, `patches/`, `playwright.config.ts`, Drizzle config, package.json (~4.2KB), and pnpm lock (~324KB).
- Status: substantial full-stack web application surface with explicit E2E infrastructure; product identity still needs source/doc inspection.

## 12. RE-UP
- Default branch: `main`
- README identifies a Relational Hierarchy Learning System blueprint/scaffold: CRSS → BITRS → ETRP → HRCO → ARASS → MFP → EVREP.
- Contents described as mathematical specification, system/repository architecture and minimal scaffold for PyTorch.
- Status: research/specification scaffold rather than proven production implementation.

## 13. Orvio
- Default branch: `main`
- Identity: hierarchical numeric optimization engine.
- Architecture described: Solver Orchestrator, Adaptive Meta-Controller and 14-phase search/refinement engine.
- Features: task typing, pluggable surrogates, uncertainty-aware LCB search, structured optimization reports, elitist basin archive.
- Status: implementation candidate; benchmark and source-level verification remain open.

## 14. Solver
- Default branch: `main`
- Identity: mathematical framework for highly symmetric combinatorial problems using short exact sequences, H1/H2 and symmetry-aware search.
- README reports 8 weights, 10 verified theorems, v2 benchmark comparisons, open problems and a closure lemma.
- Inventory rule: reported theorem/benchmark status is recorded as repository claims until independently re-executed or proven from source.

## 15. Scraper
- Default branch: `feat/developer-resources-explorer`
- Current branch root observed: `CardSkeleton.js` (~727B) and `index.html` (~18.7KB).
- Important: default branch is a feature branch, so branch identity is part of the repository card.
- Status: web implementation surface observed; overall product identity open.

## 16. Drogon
- Default branch: `feature/autorevenue-agent`
- README/package identify `autorevenue-agent` / AutoRevenue Pro.
- Stack: React, Express 5, Mongoose, Redis, JWT, Helmet, Jest, Supertest, MongoDB memory server; scripts cover frontend/backend dev and tests.
- Status: substantial application surface; performance/revenue claims are not evidence by themselves.

## 17. ai-meta-orchestrator
- Default branch: `main`
- Changelog records v1.0.0 dated 2024-07-18 with Python backend + JS frontend, linting and basic GitHub Actions CI.
- Root observed: `.github/`, `.pre-commit-config.yaml`, `CHANGELOG.md`, `agents.md`, `config/`, Docker Compose, ESLint/Jest configs, package files and `src/`.
- Status: historically versioned engineering artifact; lineage to later orchestrator systems requires explicit comparison.

## 18. training-home
- Default branch: `main`
- Root observed: dataset JSONL ~1.54MB, learning materials, `process_files.py` and GitHub Actions.
- Status: data/training workspace; model or application identity remains open.

## 19. Slack-bot
- Default branch: `main`
- Root observed: base/enhanced orchestrators, code-analysis/data-creator/documentation-analysis/embedding agents, API, deployment, production Docker Compose and docs.
- README describes OpenAI orchestration and Slack Bolt integration.
- Status: substantial multi-agent orchestration artifact; source-level architecture and test evidence remain to be examined.

## 20. The-matching-engine-
- Default branch: `main`
- Root observed: `matching_service.py` (~9.7KB), `matching_logic.md` (~8.6KB), `matching_query.sql` (~4.9KB), `schema_extensions.sql` (~4.4KB), cache client and tests.
- Status: domain implementation plus SQL/schema design; likely important for matching-related lineage, but no merge relation is inferred yet.

## 21. Doha-platform
- Default branch: `main`
- `package.json` identifies `lamsa-doha-platform` v0.1.0 with Next.js 15, React 18, Genkit, Firebase, Pexels and Zod.
- README identifies an Arabic marketplace platform and explicitly states that backend behavior is currently simulated/mock.
- Status: product prototype/full frontend surface; not yet equivalent to production backend.

## 22. -Highly-Symmetric-HAG
- Default branch: `main`
- Root contains HAG/Global-theorem subtrees, `main.py`, `minimal_bench.py`, requirements, Nemotron challenge ZIP, submission ZIP, adapter configuration and an `adapter_model.bin` of size 0.
- Status: substantial research/challenge workspace; zero-byte model artifact must not be treated as a trained model.

## Cross-batch signals introduced here
- `Meta-meta` and `Grok-supreme` are both trading/optimization-adjacent but currently have independent evidence; no lineage is asserted.
- `Symmetric-HAG`, `HAG`, `Global-theorem-`, `Solver`, and `-Highly-Symmetric-HAG` form a high-priority lineage cluster for later reconciliation, but remain separate repository cards for now.
- `Gemini-app`, `gemini-cli`, `run-gemini-cli`, `Ai-hichem`, and related agent/tool repositories form another potential lineage cluster; relation requires diff/history evidence.
- `Doha-platform`, `Doha-platform-`, `Shop-one`, `smart-delivery-platform-2`, `delivery-app`, and related marketplace/delivery repositories remain separate until direct artifact/commit comparison proves reuse or evolution.

## Evidence policy
Claims in READMEs, reports, pitch decks and benchmark tables are recorded as claims. Existence of implementation is recorded separately from execution success, reproducibility, mathematical validity, and provenance.
