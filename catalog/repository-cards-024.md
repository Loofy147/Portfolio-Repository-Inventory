# Repository Cards — Batch 024

Assessment rule: tree-first; README is narrative only. No decision is assigned here.

## Loofy147/Algorithms-
- Observed surface: `.github`, `.jules`, `AGENTS.md`, `CHANGELOG.md`, `Dockerfile`, `LICENSE`, `METHODOLOGY.md`, `README.md`, `RESOURCE_AWARE_SCHEDULER_API.md`, and a ~26KB text document on computational fundamentals.
- Identity: algorithm/computation-focused repository with explicit methodology and resource-aware scheduler documentation; implementation depth requires deeper source traversal.
- Evidence: root tree contains build/container metadata plus methodological/API documents; do not infer completion from docs.

## Loofy147/Bravolino
- Observed surface: `backend/`, `frontend/`, `docs/`, `dataset.jsonl.txt` (~1.5MB), `algerian_children_learning.png` (~1.2MB), another image, `index.html`, a 17.5MB Arabic ZIP, and Arabic marketing strategy documentation.
- Identity: Algerian children's interactive education platform with application surfaces, dataset/artifacts, product/marketing material.
- Evidence: both implementation directories and large data/archive artifacts are tracked.

## Loofy147/ClimateNow
- Current default ref: `feat/initial-project-setup`.
- Observed root surface: one `climatenow/` directory; root alone does not establish project completeness.
- Identity: climate-related project name is known, but function/implementation remains partially identified until the nested tree is inspected.

## Loofy147/A-louer
- Observed surface: `ARCHITECTURE.md`, `DATA_MODEL.md`, `DEPLOYMENT.md`, `TECHNOLOGY_STACK.md`, `UI_UX.md`, plus two Jupyter notebooks (~599KB and ~454KB).
- Identity: architecture/data-model/deployment/design workspace with executable notebooks; implementation surface requires nested/history inspection.

## Loofy147/gemini-cli
- Observed root surface: strong engineering/release controls including `.allstar`, `.gcp`, `.gemini`, `.github`, `.husky`, `.vscode`, `.nvmrc`, `.npmrc`, Prettier/YAML lint configs, and `CONTRIBUTING.md` (~19KB).
- Identity: substantial CLI/tooling repository rather than a minimal wrapper; deeper source/package inspection remains necessary before attributing capabilities.

## Loofy147/hichem-project
- Observed root surface: a single 718KB ZIP named in Arabic for “Meta Orchestrator AI specifications, vision and architecture”.
- Identity: artifact/specification repository for Meta Orchestrator AI; no source tree was observed at root in this pass.
- Important: the ZIP itself is evidence of a substantial artifact, not evidence that the packaged system is implemented.

## Loofy147/CloudCostGuard
- Observed surface: architecture, business case, domain analysis, KPI, limitations, requirements, roadmap, runbook, Dockerfile, `.github`, and README.
- Identity: cloud-cost governance/guard system with explicit operational documentation.
- Evidence maturity: unusually explicit separation of architecture, requirements, limitations and runbook; implementation depth still requires source-tree inspection.

## Loofy147/Smart-bot
- Observed root surface: `README.md` only (11 bytes).
- Identity: not established from current tree; record as minimal observed surface, not as proven empty history.

## Loofy147/El-marchi
- Observed surface: `backend/`, `frontend/`, `el-marchi-documentation.md` (~4KB), and `server_log.txt`.
- Identity: full-stack project with documented and runtime-log surfaces; function remains to be established from nested source.

## Loofy147/v0-portfolio-manager-framework
- Observed surface: Next.js-style app with `app/`, `components/`, `hooks/`, `lib/`, `public/`, `styles/`, Babel/Next/PostCSS configs, Jest config, `package.json` (~2.5KB), and a ~241KB `pnpm-lock.yaml`.
- Identity: portfolio-manager web framework/application surface; implementation exists beyond README.

## Loofy147/agentic-apis
- Observed surface: `categories/`, `data/`, `scripts/`, and README (~3.8KB).
- Identity: API/agentic resource catalogue or tooling repository; nested contents require inspection before stronger classification.
